<%= form_with(model: prov, local: true) do |form| %>
  <% if prov.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prov.errors.count, "error") %> prohibited this prov from being saved:</h2>

      <ul>
      <% prov.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prov_name %>
    <%= form.text_field :prov_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
